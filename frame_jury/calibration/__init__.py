# frame_jury/calibration — thresholds as data, per framing.
#
# SPEC.md §6: "Thresholds are data, not code. They are fitted per framing on
# the corpus and stored in calibration/, with the fit reproducible from the
# labels."
#
# M2 status: calibration is DEFERRED. No labels exist yet (SPEC.md §9, M7).
# The default thresholds in defaults.json are documented starting values,
# not fitted values.  They will be replaced when the label campaign is
# complete.  See README.md §Calibration for the fitting procedure.
